spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/${DB_NAME:gomech_staging}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: false
